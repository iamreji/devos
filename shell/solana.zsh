# ┌─────────────────────────────────────────────────────────────────┐
# │ Solana / Anchor Aliases                                         │
# └─────────────────────────────────────────────────────────────────┘

alias sb="solana build"
alias sdeploy="solana program deploy"
alias sbalance="solana balance"
alias sconfig="solana config get"
alias ssetcluster="solana config set --url"
alias ssetkey="solana config set --keypair"
alias sairdrop="solana airdrop 2"
alias stokens="solana account"

alias ab="anchor build"
alias at="anchor test"
alias ad="anchor deploy"
alias al="anchor localnet"
alias arun="anchor run"
alias aplay="anchor playground"
alias aidl="anchor idl init"
alias aidlv="anchor idl verify"
